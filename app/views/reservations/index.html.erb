<h2>Current Reservations for <%= @restaurant.name %></h2>
<table>
	<tr>
		<th>User</th>
		<th>Date</th>	
		<th>Time</th>
	</tr>
	<% @restaurant.reservations.each do |reservation| %>
		<tr>
		<td><%= reservation.user %></td>
		<td><%= reservation.datetime %></td>
		<! this one isn't working yet>
		<td><%= link_to 'Reject', restaurant_reservations_path(restaurant: @restaurant, reservation: reservation)%></td>
		</tr>
		<%end%>
</table>

<p>
<%= link_to 'Back',restaurants_path %>
</p>